package com.gs.dsa.Utility;

import com.gs.dsa.arrays.KthLargestElement;
import org.junit.jupiter.api.Test;

import static org.assertj.core.api.Assertions.assertThat;

public class MirrorTreeTest {
    @Test
    void examples(){


       // assertThat(MirrorTree.mirrorMethod(root).isEqualTo(5);
        //assertThat(MirrorTree.mirrorMethod(root).isEqualTo(4);
    }
}
